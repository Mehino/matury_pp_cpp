#include <bits/stdc++.h>
using namespace std;
int main()
    {
        int licznik = 0;
        string s;
        ifstream f("hasla.txt");
        while (f >> s) {
            s.size()
        }
    }
